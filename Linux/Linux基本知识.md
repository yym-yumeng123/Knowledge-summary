# 基本命令
```zsh
# 相对路径: 基于当前位置的目标文件路径
# . 表示当前面霜是  ./name
# .. 表示当前目录的父目录 ../name
cd 绝对路径/相对路径 # 切换当前目录

pwd  # 显示会话的当前目录

# 按字母排序 (列排序)
ls # 当前目录下的文件和目录 
ls -F # 区分文件和目录
ls -a # 隐藏文件和普通文件目录一起显示
ls -R # 列出了当前目录下包含的子目录中的文件
ls -R -F  
ls -RF
```